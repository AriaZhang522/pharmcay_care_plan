services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    # No volumes needed — in-memory, no DB

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
